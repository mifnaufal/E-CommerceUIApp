name: ecommerce
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  curved_navigation_bar: ^1.0.3
  badges: ^3.1.2 
  flutter_rating_bar: ^4.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # PASTIKAN BAGIAN INI SAMA PERSIS
  # Spasi/indentasi sangat penting
  assets:
    - assets/images/
    - assets/images/carts/